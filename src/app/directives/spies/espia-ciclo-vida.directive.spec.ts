import { EspiaCicloVidaDirective } from './espia-ciclo-vida.directive';

describe('EspiaCicloVidaDirective', () => {
  it('should create an instance', () => {
    const directive = new EspiaCicloVidaDirective();
    expect(directive).toBeTruthy();
  });
});
