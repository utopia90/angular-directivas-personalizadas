import { RenderizadoCondicionalDirective } from './renderizado-condicional.directive';

describe('RenderizadoCondicionalDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderizadoCondicionalDirective();
    expect(directive).toBeTruthy();
  });
});
